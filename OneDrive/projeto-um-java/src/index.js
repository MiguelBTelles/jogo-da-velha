import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Opcional, se você planeja usar CSS
import App from './app';
import './styles.css';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
